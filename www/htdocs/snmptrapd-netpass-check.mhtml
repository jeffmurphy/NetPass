<%doc>
</%doc>
<%perl>
	use Proc::ProcessTable;
	my $pt = new Proc::ProcessTable();
	foreach my $p (@{$pt->table}) {
		if ($p->cmndline =~ /snmptrapd/) {
			print "SNMPTRAPD-OK\n";
			return;
		}
	}
	print "SNMPTRAPD-NOK\n";
</%perl>
